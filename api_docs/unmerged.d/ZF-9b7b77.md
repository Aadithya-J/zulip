* Added the `week_starts_on` user setting to the `GET /settings` and `POST /register`
  responses, and documented the corresponding `PATCH /settings` parameter, so
  clients can configure the first day of the week used in Zulip date pickers.
